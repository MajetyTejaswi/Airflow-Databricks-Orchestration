name: Deploy Airflow Infrastructure & DAGs

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'airflow-dags/**'
      - 'terraform/**'
      - '.github/workflows/deploy.yml'
      - 'requirements.txt'
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  TERRAFORM_VERSION: 1.6.0
  PYTHON_VERSION: '3.11'
  AIRFLOW_VERSION: 2.7.3

jobs:
  # Job 1: Validate Terraform
  validate-terraform:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Format Check
        run: |
          cd terraform
          terraform fmt -check -recursive

      - name: Terraform Validate
        run: |
          cd terraform
          terraform init -backend=false
          terraform validate

  # Job 2: Validate DAGs
  validate-dags:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install apache-airflow==${{ env.AIRFLOW_VERSION }}

      - name: Validate DAGs syntax
        run: |
          export AIRFLOW_HOME=$(pwd)/airflow_test
          mkdir -p $AIRFLOW_HOME
          airflow db init --quiet 2>/dev/null || true
          
          for dag_file in airflow-dags/*.py; do
            echo "Validating $dag_file..."
            python -m py_compile "$dag_file"
          done
          
          echo "All DAGs validated successfully!"

      - name: Check DAG imports
        run: |
          python3 << 'EOF'
          import sys
          import os
          sys.path.insert(0, os.getcwd())
          
          dag_files = [f for f in os.listdir('airflow-dags') if f.endswith('.py')]
          
          for dag_file in dag_files:
              print(f"Checking imports in {dag_file}...")
              try:
                  with open(f'airflow-dags/{dag_file}', 'r') as f:
                      exec(compile(f.read(), dag_file, 'exec'), {'__name__': '__main__'})
                  print(f"  ✓ {dag_file} imports OK")
              except Exception as e:
                  print(f"  ✗ {dag_file} import error: {e}")
                  sys.exit(1)
          EOF

  # Job 3.5: Setup Terraform Backend
  setup-terraform-backend:
    needs: [validate-terraform]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    outputs:
      bucket_name: ${{ steps.backend-setup.outputs.bucket_name }}
      dynamodb_table: ${{ steps.backend-setup.outputs.dynamodb_table }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform Backend Infrastructure
        id: backend-setup
        run: |
          chmod +x scripts/setup-terraform-backend.sh
          scripts/setup-terraform-backend.sh
          
          # Export outputs
          AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          BUCKET_NAME="airflow-terraform-state-${AWS_ACCOUNT_ID}"
          DYNAMODB_TABLE="airflow-terraform-locks"
          
          echo "bucket_name=$BUCKET_NAME" >> $GITHUB_OUTPUT
          echo "dynamodb_table=$DYNAMODB_TABLE" >> $GITHUB_OUTPUT
          echo "AWS_ACCOUNT_ID=$AWS_ACCOUNT_ID" >> $GITHUB_OUTPUT

  # Job 3: Plan Terraform
  plan-terraform:
    needs: [setup-terraform-backend]
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || (github.ref == 'refs/heads/develop' && github.event_name == 'push')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Create Terraform Backend Config
        run: |
          cd terraform
          cat > backend-config.tfbackend << EOF
          bucket         = "${{ needs.setup-terraform-backend.outputs.bucket_name }}"
          key            = "airflow/terraform.tfstate"
          region         = "${{ env.AWS_REGION }}"
          encrypt        = true
          dynamodb_table = "${{ needs.setup-terraform-backend.outputs.dynamodb_table }}"
          EOF

      - name: Terraform Plan
        run: |
          cd terraform
          terraform init -backend-config=backend-config.tfbackend
          terraform plan -out=tfplan -no-color
          
      - name: Comment PR with plan
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('terraform/tfplan', 'utf8');
            const comment = `## Terraform Plan\n\n\`\`\`\n${plan}\n\`\`\``;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment.substring(0, 65536) // GitHub comment limit
            });

  # Job 4: Deploy Infrastructure (only on main branch)
  deploy-infrastructure:
    needs: [validate-terraform, validate-dags, setup-terraform-backend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Create Terraform Backend Config
        run: |
          cd terraform
          cat > backend-config.tfbackend << EOF
          bucket         = "${{ needs.setup-terraform-backend.outputs.bucket_name }}"
          key            = "airflow/terraform.tfstate"
          region         = "${{ env.AWS_REGION }}"
          encrypt        = true
          dynamodb_table = "${{ needs.setup-terraform-backend.outputs.dynamodb_table }}"
          EOF
          cat backend-config.tfbackend

      - name: Terraform Init
        run: |
          cd terraform
          terraform init -backend-config=backend-config.tfbackend

      - name: Terraform Plan
        run: |
          cd terraform
          terraform plan -out=tfplan -no-color

      - name: Terraform Apply
        run: |
          cd terraform
          terraform apply tfplan -no-color
          
          # Save outputs to artifacts
          terraform output -json > outputs.json
          cat outputs.json
          
          # Display Airflow access info
          echo ""
          echo "=========================================="
          terraform output -raw airflow_access_info
          echo "=========================================="

      - name: Save SSH Private Key
        run: |
          cd terraform
          # The private key is generated at terraform/airflow-key.pem
          if [ -f airflow-key.pem ]; then
            echo "✓ SSH private key generated successfully"
            chmod 600 airflow-key.pem
          else
            echo "⚠ Warning: SSH private key not found"
          fi

      - name: Upload SSH Key
        uses: actions/upload-artifact@v4
        with:
          name: ssh-key
          path: terraform/airflow-key.pem
          retention-days: 1

      - name: Upload Terraform outputs
        uses: actions/upload-artifact@v4
        with:
          name: terraform-outputs
          path: terraform/outputs.json
          retention-days: 1

      - name: Notify deployment success
        run: |
          echo "✓ Infrastructure deployed successfully!"
          cd terraform
          EC2_IP=$(terraform output -raw ec2_public_ip 2>/dev/null || echo 'N/A')
          echo "EC2 IP: $EC2_IP"

  # Job 5.5: Wait for EC2 to be ready
  wait-for-ec2:
    needs: [deploy-infrastructure]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Download Terraform outputs
        uses: actions/download-artifact@v4
        with:
          name: terraform-outputs

      - name: Wait for EC2 to be ready
        run: |
          INSTANCE_ID=$(cat outputs.json | jq -r '.ec2_instance_id.value // empty' 2>/dev/null || echo "")
          
          if [ -z "$INSTANCE_ID" ]; then
            echo "⚠ EC2 Instance ID not found in outputs"
            exit 0
          fi
          
          echo "Checking EC2 instance: $INSTANCE_ID"
          
          # Check if instance is running
          STATE=$(aws ec2 describe-instances --instance-ids $INSTANCE_ID --region ${{ env.AWS_REGION }} --query 'Reservations[0].Instances[0].State.Name' --output text)
          echo "Current state: $STATE"
          
          if [ "$STATE" != "running" ]; then
            echo "Starting EC2 instance..."
            aws ec2 start-instances --instance-ids $INSTANCE_ID --region ${{ env.AWS_REGION }} || true
            aws ec2 wait instance-running --instance-ids $INSTANCE_ID --region ${{ env.AWS_REGION }}
          fi
          
          echo "✓ EC2 instance is running"
          
          # Wait for status checks
          echo "Waiting for instance status checks..."
          aws ec2 wait instance-status-ok --instance-ids $INSTANCE_ID --region ${{ env.AWS_REGION }} || echo "Status checks taking longer than expected, continuing anyway..."
          
          echo "✓ EC2 instance is ready"

  # Job 5: Deploy DAGs to EC2
  deploy-dags:
    needs: [validate-dags, wait-for-ec2]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Terraform outputs
        uses: actions/download-artifact@v4
        with:
          name: terraform-outputs

      - name: Download SSH Key
        uses: actions/download-artifact@v4
        with:
          name: ssh-key
          path: ~/.ssh/

      - name: Extract EC2 IP
        id: get-ec2-ip
        run: |
          EC2_IP=$(cat outputs.json | grep -o '"ec2_public_ip": "[^"]*"' | cut -d'"' -f4 || echo "")
          if [ -z "$EC2_IP" ]; then
            EC2_IP=$(cat outputs.json | jq -r '.ec2_public_ip.value // empty' 2>/dev/null || echo "")
          fi
          echo "ec2_ip=$EC2_IP" >> $GITHUB_OUTPUT
          echo "EC2 IP: $EC2_IP"

      - name: Setup SSH
        run: |
          chmod 600 ~/.ssh/airflow-key.pem
          ssh-keyscan -H ${{ steps.get-ec2-ip.outputs.ec2_ip }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Wait for EC2 SSH
        run: |
          MAX_ATTEMPTS=30
          ATTEMPT=1
          
          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
            if ssh -i ~/.ssh/airflow-key.pem -o ConnectTimeout=5 -o StrictHostKeyChecking=no ubuntu@${{ steps.get-ec2-ip.outputs.ec2_ip }} "echo 'SSH ready'" 2>/dev/null; then
              echo "✓ SSH is ready"
              break
            fi
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: Waiting for SSH..."
            sleep 10
            ATTEMPT=$((ATTEMPT + 1))
          done

      - name: Deploy DAGs to EC2
        run: |
          scp -i ~/.ssh/airflow-key.pem -o StrictHostKeyChecking=no -r airflow-dags/* ubuntu@${{ steps.get-ec2-ip.outputs.ec2_ip }}:/home/airflow/airflow/dags/ 2>/dev/null || true
          echo "✓ DAGs deployed to EC2"

      - name: Deploy Notebooks to EC2
        run: |
          scp -i ~/.ssh/airflow-key.pem -o StrictHostKeyChecking=no -r databricks-notebooks/* ubuntu@${{ steps.get-ec2-ip.outputs.ec2_ip }}:/tmp/databricks-notebooks/ 2>/dev/null || true
          echo "✓ Notebooks staged on EC2"

      - name: Restart Airflow services
        run: |
          ssh -i ~/.ssh/airflow-key.pem -o StrictHostKeyChecking=no ubuntu@${{ steps.get-ec2-ip.outputs.ec2_ip }} << 'EOF'
          echo "Restarting Airflow services..."
          sudo systemctl restart airflow-scheduler || echo "Scheduler restart attempted"
          sudo systemctl restart airflow-webserver || echo "Webserver restart attempted"
          
          sleep 10
          
          echo "Verifying services..."
          sudo systemctl status airflow-scheduler --no-pager || true
          sudo systemctl status airflow-webserver --no-pager || true
          
          echo "✓ Airflow services updated"
          EOF

  # Job 6: Sync DAGs via Git (Alternative method)
  sync-dags-git:
    needs: [validate-dags, wait-for-ec2]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Terraform outputs
        uses: actions/download-artifact@v4
        with:
          name: terraform-outputs

      - name: Download SSH Key
        uses: actions/download-artifact@v4
        with:
          name: ssh-key
          path: ~/.ssh/

      - name: Setup SSH for Git sync
        run: |
          chmod 600 ~/.ssh/airflow-key.pem

      - name: Trigger Git sync on EC2
        run: |
          EC2_IP=$(cat outputs.json | jq -r '.ec2_public_ip.value // empty' 2>/dev/null || echo "")
          if [ -z "$EC2_IP" ]; then
            EC2_IP=$(cat outputs.json | grep -o '"ec2_public_ip": "[^"]*"' | cut -d'"' -f4 || echo "")
          fi
          
          if [ -z "$EC2_IP" ]; then
            echo "ℹ Skipping Git sync - EC2 IP not available"
            exit 0
          fi
          
          echo "Syncing DAGs from Git on EC2..."
          ssh -i ~/.ssh/airflow-key.pem -o StrictHostKeyChecking=no ubuntu@$EC2_IP << 'EOF'
          cd /home/airflow/airflow/dags 2>/dev/null || true
          if [ -d ".git" ]; then
            git pull origin main
            echo "✓ DAGs synced from Git"
          else
            echo "ℹ Not a git repository"
          fi
          EOF

  # Job 7: Validate deployment
  validate-deployment:
    needs: [deploy-dags]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Terraform outputs
        uses: actions/download-artifact@v4
        with:
          name: terraform-outputs

      - name: Download SSH Key
        uses: actions/download-artifact@v4
        with:
          name: ssh-key
          path: ~/.ssh/

      - name: Setup SSH
        run: |
          chmod 600 ~/.ssh/airflow-key.pem

      - name: Validate DAGs on EC2
        run: |
          EC2_IP=$(cat outputs.json | jq -r '.ec2_public_ip.value // empty' 2>/dev/null || echo "")
          if [ -z "$EC2_IP" ]; then
            EC2_IP=$(cat outputs.json | grep -o '"ec2_public_ip": "[^"]*"' | cut -d'"' -f4 || echo "")
          fi
          
          if [ -z "$EC2_IP" ]; then
            echo "⚠ EC2 IP not available, skipping validation"
            exit 0
          fi
          
          echo "Validating DAGs on EC2: $EC2_IP"
          ssh -i ~/.ssh/airflow-key.pem -o StrictHostKeyChecking=no ubuntu@$EC2_IP << 'EOF'
          export AIRFLOW_HOME=/home/airflow/airflow
          source $AIRFLOW_HOME/venv/bin/activate 2>/dev/null || true
          
          echo "Listing available DAGs..."
          airflow dags list 2>/dev/null || echo "DAGs validation not available yet"
          echo "✓ Deployment validation complete"
          EOF

      - name: Create deployment summary
        run: |
          echo "## Deployment Summary ✓" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Deployment Status" >> $GITHUB_STEP_SUMMARY
          echo "- ✓ Infrastructure: Deployed and running" >> $GITHUB_STEP_SUMMARY
          echo "- ✓ Services: Started (EC2, RDS, ECS)" >> $GITHUB_STEP_SUMMARY
          echo "- ✓ DAGs: Deployed and synced" >> $GITHUB_STEP_SUMMARY
          echo "- ✓ Services: Restarted" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          EC2_IP=$(cat outputs.json | jq -r '.ec2_public_ip.value // empty' 2>/dev/null || echo "N/A")
          [ "$EC2_IP" != "N/A" ] && echo "**Airflow UI**: http://$EC2_IP:8080" >> $GITHUB_STEP_SUMMARY

  # Job 8: Notify on failure
  notify-failure:
    runs-on: ubuntu-latest
    if: failure()
    needs: [validate-terraform, validate-dags, deploy-infrastructure, wait-for-ec2]
    steps:
      - name: Send Slack notification
        uses: slackapi/slack-github-action@v1.24.0
        with:
          webhook-url: ${{ secrets.SLACK_WEBHOOK }}
          payload: |
            {
              "text": "❌ Airflow Deployment Failed",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Deployment Failed* 🚨\n*Repository*: ${{ github.repository }}\n*Branch*: ${{ github.ref }}\n*Commit*: ${{ github.sha }}\n*Author*: ${{ github.actor }}"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Run*: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Details>"
                  }
                }
              ]
            }
        if: always()

        
